---
sidebar_position: 0
---
# Power
## Shutdown now
```bash
sudo shutdown now
```
## Scheduled shutdown
```bash
sudo shutdown +15 #15 mins
sudo shutdown -h 11:00 # at 11:00
sudo shutdown -c # cancel
```
## Reboot
```bash
sudo reboot now
```
